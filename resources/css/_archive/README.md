# CSS archive (orphans)

Folder giữ chỗ cho stylesheet **không** load qua:

- `resources/js/lib/pushsaleStyleRegistry.js`
- Vite entrypoints (`app.css`, `public.css`, `pushsale.css`)
- Direct page imports

Các file CSS orphan đã được xóa (2026-09-17). Chỉ giữ `README.md` này.
Không nhặt CSS cũ từ git history vào registry trừ khi có lý do rõ.

Active CSS policy: `docs/AGENTS.md` + `docs/PROJECT_CONTRACT.md`.
