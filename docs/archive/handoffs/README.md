# Archived handoffs

Versioned prompt notes (`CONTEXT_HANDOFF_V*`, `UI_*_V*`, `PUSHSALE_TEMPLATE_V*`, …) moved here during project cleanup.

**Current contract:** `docs/PROJECT_CONTRACT.md` + root `AGENTS.md`.

Do not add new versioned handoffs. Update the living contract instead.
