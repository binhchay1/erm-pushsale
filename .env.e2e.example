# Copy to .env.e2e (gitignored) or export in shell before `pnpm e2e`.
# Local:
# E2E_BASE_URL=http://127.0.0.1:8000
# Production (real data):
E2E_BASE_URL=https://salesloop.vn
E2E_EMAIL=superadmin@saleops.local
E2E_PASSWORD=password

# Full demo pipeline (serial):
#   pnpm e2e:demo
#
# All flow specs:
#   pnpm e2e:flows
